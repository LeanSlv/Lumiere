@model List<int>

    <select id="roomNumber" class="form-control" onload="loadPrice($('#roomNumber'));">
        @foreach (int roomNumber in Model)
        {
            <option value=@roomNumber>@roomNumber</option>
        }
    </select>