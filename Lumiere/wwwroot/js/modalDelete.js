function userDeletionConfirm(id) {
    $('#userDeleteModal').modal('show');
    $('#user_Id').val(id);
}

function filmDeletionConfirm(id) {
    $('#filmDeleteModal').modal('show');
    $('#film_Id').val(id);
}

function seanceDeletionConfirm(id) {
    $('#seanceDeleteModal').modal('show');
    $('#seance_Id').val(id);
}

function reservedSeatDeletionConfirm(id) {
    $('#reservedSeatDeleteModal').modal('show');
    $('#seat_Id').val(id);
}

